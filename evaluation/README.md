# Evaluation Artifacts

This directory contains recruiter-facing evaluation materials that complement the in-app `/benchmark` route.

- `datasets/` contains the seeded benchmark dataset used for product-level evaluation
- `results/` contains derived summary metrics and flagged review cases
- `plots/` is reserved for exported chart images if you want to add static visuals later
- `scripts/` is reserved for future artifact regeneration scripts
